VITE_API_URL=/api/v1
# The NestJS server's own origin - needed for the notifications WebSocket
# (Socket.IO connects directly, not through the Vite dev proxy, which only
# proxies /api). Defaults to http://localhost:3000 in code if unset.
VITE_WS_URL=http://localhost:3000
